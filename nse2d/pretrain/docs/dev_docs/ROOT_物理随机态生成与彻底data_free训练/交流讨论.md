# 交流讨论

## 元信息

- QID：Q01
- 最近更新时间：2026-03-11

## 讨论条目

| 日期 | 对象 | 主题 | 关键反馈 | 结论 | 未决问题 | 后续动作 |
|------|------|------|----------|------|----------|----------|
| 2026-03-11 | 用户 + Agent | `dev_docs` 中主问题命名 | 当前主问题应更直接写成“更符合物理的随机生成数据并训练，实现彻底的 data-free 模型” | 将 Q01 从泛化的 strict data-free 问题，改写为“物理随机态生成与彻底 data-free 训练” | 目录名是否还要更短 | 已按新名字重建 Q01 文档 |
| 2026-03-11 | 用户 + Agent | 当前最关键的子问题 | 用户明确指出应重点盯住：`batch_size` 影响、为何 staged 当前最好、为何 GRF 训练 loss 与单步变好但多步变差、以及 rollout 爆炸 pattern | 这些都已被提升为 Q01 的核心子问题，而不是零散备注 | 爆炸 pattern 还缺结构化图档 | 后续实验与图表需要回写到 `实验记录.md` |
| 2026-03-11 | 用户 + Agent | 候选技术路线 | 用户明确给出当前候选方案：生成模型、learnable GRF、self-training bootstrap | 三条路线已写入 `深度思考.md` 和 `调研记录.md` | 哪条是主线，哪条是 bridge 仍待后续实验 | 继续补路线比较 |
| 2026-03-11 | 用户 + Agent | 当前随机态约束 | 用户强调 `n` 场和 `Ti` 场不需要绝对值限制 | 先记为重要设计观察，不直接写成硬事实 | 还缺系统性验证 | 在后续生成器实验中加入 field-specific 约束对照 |

## 当前影响

- 改变了哪些判断：
  - 主问题命名被重写得更贴近真实研究目标。
  - `batch_size`、staged 最优、single-step vs multi-step 分裂、爆炸 pattern 都被显式提升为主问题子项。
- 没改变哪些判断：
  - 当前最好经验路线仍然不是 strict data-free 已完成。
  - offline / staged 仍然是不可绕开的参照系。

## 下一步

- 需要谁跟进：
  - 用户或 Agent 后续每次只要改变了候选路线排序、field 约束设计或对爆炸 pattern 的解释，就应回写这里。
- 是否回写 `README.md` 或 `main.md`：
  - 需要，且本次已经同步回写。
