# 交流讨论

> 何时使用：外部反馈或讨论改变判断、暴露新动作、或需要保存关键争议点时创建。

## 元信息

- QID：Q01
- 最近更新时间：2026-03-11

## 讨论条目

| 日期 | 对象 | 主题 | 关键反馈 | 结论 | 未决问题 | 后续动作 |
|------|------|------|----------|------|----------|----------|
| 2026-03-11 | 用户任务说明 + Agent | Q01 迁移范围 | 旧 ROOT 目录已改成 `ROOT_物理随机态生成与彻底data_free训练`；新 Q01 只负责 literature / existing methods / major challenges / later split | 使用 `Q01_lit_method_map/` 作为 Q01 稳定工作区，并只迁移上游内容 | `main.md` 仍指向旧 ROOT；本轮任务不允许修改 | 在 `README.md` 与 `sub_main.md` 中明确 Q01 边界与后续拆分 |
| 2026-03-11 | 用户任务说明 + Agent | Q01 文件触发器 | Q01 已有外部资料需求、方法比较需求和会改变判断的讨论 | 启用 `调研记录.md`、`深度思考.md`、`交流讨论.md`，并用 `sub_main.md` 管理当前进度；不启用 `实验记录.md`、`理论推导.md` | 后续若出现实验 run 或正式推导，再单独启用对应文件 | 先按最小集合和进度控制面板落盘 |
| 2026-03-11 | 用户 + Agent（来自旧 ROOT 讨论） | 当前关键 challenge seeds | `batch_size` 影响、why staged best、why one-step 与 multi-step 分裂、rollout 爆炸 pattern 都应被显式保留 | 这些先作为 later split 候选，不在 Q01 内提前定论 | 哪些属于 `Q02`，哪些属于具体路线仍未定 | 继续在 `调研记录.md` 中维护映射 |
| 2026-03-11 | 用户 + Agent（来自旧 ROOT 讨论） | 候选方法族 | 当前内部至少有生成模型、learnable GRF、bootstrap / self-training 三条 strict data-free 候选线 | Q01 先维护方法族与角色排序，路线验证下沉到 `Q04-Q06` | 外部 literature 对应关系还没补齐 | 在后续调研中补 representative papers |

## 当前影响

- 改变了哪些判断：
  - Q01 不再承载整个根问题，只承载上游的文献 / 方法 / 挑战 / 拆分问题。
  - route comparison 以“后续问题如何拆”为目的，而不是在这里直接宣判实验胜负。
- 没改变哪些判断：
  - 真实状态锚点仍是当前最强参考系。
  - strict data-free 仍未解决。

## 下一步

- 需要谁跟进：
  - 用户或 Agent 在补完 external literature 后，需要更新方法图谱和 subproblem tree。
  - 如果 `batch_size`、why staged best、one-step vs rollout、爆炸 pattern 的归属发生变化，需要继续回写本文件。
- 是否回写 `README.md`、`sub_main.md` 或 `main.md`：
  - 已回写 `README.md`，并用 `sub_main.md` 维护 Q01 当前进度与状态。
  - `main.md` 本轮按用户要求不改。
